version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 16
        - npm ci --legacy-peer-deps
    build:
      commands:
        - export NODE_OPTIONS="--openssl-legacy-provider"
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*